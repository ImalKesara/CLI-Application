A sample command-line application with an entrypoint in `bin/`, library code
in `lib/`, and example unit test in `test/`.

What are the differences of /lib and /lib/src
lib/ -> public interface
src/ -> private implementation

concrete implementation
a class / method that has the actual working code , not just a description of what it should do

abstract = blueprint(what must exist)
concrete = real working version(how it works)

